name: Linting with black

on:
  push:
      paths-ignore:
        - 'main**'
  pull_request:
      paths-ignore:
        - 'main**'
      
jobs:

  black-linting:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - uses: psf/black@stable
